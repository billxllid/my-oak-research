{
  "name": "worker",
  "private": true,
  "scripts": {
    "dev": "tsx index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx index.ts",
    "lint": "echo 'worker lint not configured'",
    "test": "echo 'worker tests not configured'",
    "check-types": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5"
  }
}
